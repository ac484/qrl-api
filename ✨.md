# 交易系統架構指南

> **Domain 完全不知道 FastAPI / Redis / Supabase / HTTP / WebSocket 的存在**  
> **Application 編排用例**  
> **Infrastructure 只負責技術實作**  
> **Interfaces 只負責 I/O 與傳輸**

---

## 目錄

1. [核心架構原則](#一核心架構原則)
2. [分層職責定義](#二分層職責定義)
3. [交易系統專用設計](#三交易系統專用設計)
4. [多策略協調機制](#四多策略協調機制)
5. [實時數據流整合](#五實時數據流整合)
6. [生產環境部署](#六生產環境部署)
7. [完整實現範例](#七完整實現範例)

---

## 一、核心架構原則

### 1.1 分層架構總覽

```
┌────────────────────────────┐
│        Interfaces          │  ← FastAPI / WS / REST / CLI
└────────────▲───────────────┘
             │
┌────────────┴───────────────┐
│        Application         │  ← Use Cases / Services / Orchestration
└────────────▲───────────────┘
             │
┌────────────┴───────────────┐
│           Domain           │  ← Business Truth
└────────────▲───────────────┘
             │
┌────────────┴───────────────┐
│        Infrastructure      │  ← Redis / MEXC / Supabase / HTTP
└────────────────────────────┘
```

**依賴只能「往內」**:
- `Interfaces → Application → Domain`
- `Infrastructure → Domain (透過介面)`

### 1.2 核心設計原則

#### 絕對準則
> **檔案大小不是問題，責任邊界才是**  
> **只要 Domain 乾淨，你的系統可以一直活下去**  
> **遵守上述兩點，單一檔案很難超過4000字元**

#### 交易系統核心原則
> **策略不下單，持倉不碰 API，下單不懂策略**

也就是：
- **Strategy**: 只產生「意圖（Intent / Signal）」
- **Position**: 只負責「狀態與風控」
- **Execution**: 只負責「怎麼送單」
- **Infrastructure**: 只負責「怎麼連交易所」

這樣你才能：
- 同一策略 → 模擬 / 實盤
- 同一策略 → 多交易所
- 同一持倉邏輯 → 多策略共用

### 1.3 理想目錄結構

```text
src/app/
├── domain/                # 🔴 絕對純粹
│   ├── entities/
│   ├── value_objects/
│   ├── aggregates/
│   ├── services/
│   ├── events/
│   ├── repositories.py    # interface only
│   └── errors.py
│
├── application/           # 🟠 用例編排
│   ├── use_cases/
│   ├── services/
│   ├── dtos/
│   ├── commands/
│   ├── queries/
│   └── ports.py           # outbound ports
│
├── infrastructure/        # 🟡 技術實作
│   ├── mexc/
│   │   ├── http_client.py
│   │   ├── ws_client.py
│   │   ├── signer.py
│   │   └── adapters.py
│   ├── redis/
│   │   ├── client.py
│   │   └── cache_repo.py
│   ├── supabase/
│   │   ├── client.py
│   │   └── user_repo.py
│   └── persistence/
│
├── interfaces/            # 🟢 傳輸層
│   ├── http/
│   │   ├── routes.py
│   │   ├── schemas.py     # Pydantic
│   │   └── deps.py
│   ├── websocket/
│   └── background/
│
└── shared/                # ⚪ 跨層共用
    ├── config.py
    ├── time.py
    ├── logging.py
    └── utils.py
```

---

## 二、分層職責定義

### 2.1 Domain 層（🔴 最重要）

#### ✅ 可以做的事

- Entity / Aggregate
- Business Rule
- Domain Event
- Repository Interface（抽象）

#### ❌ 絕對不可以

- `fastapi`
- `redis`
- `supabase`
- `httpx`
- `protobuf`
- `websockets`
- `pydantic`

#### 示例代碼

```python
# domain/repositories.py
from abc import ABC, abstractmethod

class OrderRepository(ABC):
    @abstractmethod
    async def save(self, order): ...
```

### 2.2 Application 層（🟠 系統用例）

#### ✅ 可以做的事

- Use Case
- 任務流程（例如：下單 → 等回報 → 快取）
- DTO
- Transaction boundary
- Port 定義

#### ❌ 不可以做的事

- 直接用 redis / supabase client
- 直接呼叫 mexc API

#### 示例代碼

```python
class PlaceOrderUseCase:
    def __init__(self, order_repo, exchange_port):
        ...
```

### 2.3 Infrastructure 層（🟡 技術地獄）

#### ✅ 可以做的事

- **實作** Repository
- 呼叫 MEXC API
- Redis Cache
- Supabase 存取
- protobuf decode

#### ❌ 不可以做的事

- 商業邏輯
- if / else 決策規則

#### 示例代碼

```python
class RedisOrderRepository(OrderRepository):
    async def save(self, order):
        await self.redis.set(...)
```

### 2.4 Interfaces 層（🟢 世界入口）

#### ✅ 可以做的事

- FastAPI Router
- WebSocket handler
- Request / Response Schema
- Auth / Dependency

#### ❌ 不可以做的事

- 商業邏輯
- 資料庫邏輯

#### 示例代碼

```python
@router.post("/orders")
async def place_order(cmd: PlaceOrderRequest):
    await use_case.execute(cmd)
```

### 2.5 Shared 層（⚪ 很容易濫用）

#### ✅ 只能放

- config
- logging
- time
- serializer
- retry / backoff

#### ❌ 不能放

- 業務規則
- Entity
- Repository

### 2.6 套件對應層級

| 套件           | 層                        |
| ------------ | ------------------------ |
| fastapi      | interfaces               |
| pydantic     | interfaces / application |
| httpx        | infrastructure           |
| websockets   | infrastructure           |
| protobuf     | infrastructure           |
| redis        | infrastructure           |
| supabase     | infrastructure           |
| cryptography | infrastructure           |
| orjson       | infrastructure           |
| pytest       | 全層（測試）                   |

### 2.7 架構崩壞檢查

#### ❌ 出現這種 import → 架構已壞

```python
# domain
import fastapi
import redis
```

#### ❌ Application 直接 new Redis

```python
redis = Redis(...)
```

#### ✅ 正確

```python
def __init__(self, repo: OrderRepository):
    ...
```

---

## 三、交易系統專用設計

### 3.1 完整目錄結構

```text
src/app/
├── domain/                             # 🔴 業務真理（不碰技術）
│   ├── trading/
│   │   ├── entities/
│   │   │   ├── order.py
│   │   │   ├── trade.py
│   │   │   ├── position.py             # 智能持倉核心
│   │   │   └── account.py
│   │   │
│   │   ├── value_objects/
│   │   │   ├── symbol.py
│   │   │   ├── price.py
│   │   │   ├── quantity.py
│   │   │   ├── leverage.py
│   │   │   └── timeframe.py
│   │   │
│   │   ├── strategies/
│   │   │   ├── base.py                 # Strategy 抽象
│   │   │   ├── signal.py               # Buy / Sell / Hold
│   │   │   └── indicators/
│   │   │
│   │   ├── services/
│   │   │   ├── risk_service.py
│   │   │   └── position_service.py
│   │   │
│   │   ├── events/
│   │   │   ├── signal_generated.py
│   │   │   ├── order_requested.py
│   │   │   ├── order_filled.py
│   │   │   └── position_updated.py
│   │   │
│   │   ├── repositories.py             # interface only
│   │   └── errors.py
│
├── application/                        # 🟠 用例 / 任務編排
│   ├── trading/
│   │   ├── use_cases/
│   │   │   ├── generate_signal.py
│   │   │   ├── manage_position.py
│   │   │   ├── place_order.py
│   │   │   └── sync_exchange_state.py
│   │   │
│   │   ├── services/
│   │   │   ├── trading_bot_service.py  # 機器人主流程
│   │   │   └── execution_service.py
│   │   │
│   │   ├── ports/
│   │   │   ├── exchange_port.py        # 下單/行情抽象
│   │   │   ├── market_data_port.py
│   │   │   └── position_repo_port.py
│   │   │
│   │   ├── dtos/
│   │   │   ├── signal_dto.py
│   │   │   ├── order_dto.py
│   │   │   └── position_dto.py
│   │   └── commands/
│   │
├── infrastructure/                     # 🟡 技術實作
│   ├── exchange/
│   │   └── mexc/
│   │       ├── rest_client.py          # httpx
│   │       ├── ws_client.py            # websockets
│   │       ├── signer.py               # 簽名封裝
│   │       ├── adapters.py             # implements ports
│   │       └── protobuf_decoder.py
│   │
│   ├── redis/
│   │   ├── client.py
│   │   ├── position_cache.py
│   │   └── lock.py
│   │
│   ├── supabase/
│   │   ├── client.py
│   │   ├── trade_repo.py
│   │   └── position_repo.py
│   │
│   └── scheduler/
│       └── jobs.py
│
├── interfaces/                         # 🟢 對外入口
│   ├── templates/
│   │   ├── dashboard.html
│   │   └── static/
│   │       └── js
│   ├── http/
│   │   ├── routes/
│   │   │   ├── trading.py
│   │   │   └── account.py
│   │   ├── schemas.py
│   │   └── deps.py
│   │
│   ├── websocket/
│   │   └── market_stream.py
│   │
│   └── cli/
│       └── run_bot.py
│
└── shared/                             # ⚪ 技術共用
    ├── config.py
    ├── logging.py
    ├── time.py
    ├── retry.py
    └── constants.py
```

### 3.2 各層交易系統責任

#### 🔴 Domain（策略 & 持倉的家）

✅ 可以
- Strategy → 產生 Signal
- Position → 計算浮盈 / 加減倉 / 停損
- Risk Rule（最大倉位、槓桿）

❌ 不可以
- API Key
- Redis
- HTTP
- WS
- JSON / protobuf

```python
class Strategy(ABC):
    def generate(self, candles) -> Signal:
        ...
```

#### 🟠 Application（機器人大腦）

**交易流程只存在這一層**

```text
行情 → 策略 → Signal
Signal → 持倉風控 → Order Intent
Order Intent → Execution
```

```python
class TradingBotService:
    async def on_market_tick(self, market_data):
        signal = self.strategy.generate(market_data)
        order = self.position.apply(signal)
        if order:
            await self.execution.place(order)
```

#### 🟡 Infrastructure（交易所地獄）

**MEXC 封裝「三層拆開」**

```text
signer.py        # 只負責 HMAC
rest_client.py   # 只負責 HTTP
adapters.py      # 只負責轉 Domain Model
```

```python
class MexcExchangeAdapter(ExchangePort):
    async def place_order(self, order):
        payload = map_order(order)
        return await self.client.post("/order", payload)
```

#### 🟢 Interfaces（人類 & 外部世界）

FastAPI 只做：
- 驗證
- DTO 轉換
- 呼叫 UseCase

```python
@router.post("/bot/start")
async def start_bot():
    await trading_bot.start()
```

### 3.3 策略/持倉/下單交互

```text
Strategy
   ↓ Signal
Position (智能持倉)
   ↓ OrderIntent
ExecutionService
   ↓
ExchangePort
```

**策略永遠不知道**:
- 你有沒有錢
- 下單成功與否

**Position 永遠不知道**:
- 用哪個交易所
- API 長怎樣

### 3.4 MEXC 簽名與 API 封裝

```text
infrastructure/exchange/mexc/
├── signer.py        # HMAC SHA256
├── rest_client.py   # httpx
├── ws_client.py
└── adapters.py
```

```python
class MexcSigner:
    def sign(self, params: dict) -> dict:
        ...
```

**Application / Domain 永遠看不到 signer**

### 3.5 快速自檢表

| 檢查項目              | 正確答案 |
| --------------- | ---- |
| 策略裡出現 `httpx`    | ❌    |
| Position 裡出現 API Key | ❌    |
| FastAPI 裡寫風控      | ❌    |
| Infrastructure 裡有 if 決策邏輯 | ❌    |
| Strategy 只回 Signal | ✅    |
| Position 決定加減倉    | ✅    |
| Execution 只送單     | ✅    |
| Adapter 只轉換資料    | ✅    |

---

## 四、多策略協調機制

### 4.1 一句話定義

> **策略只表達「我想怎樣」**  
> **持倉負責「能不能、要不要、怎麼做」**

### 4.2 核心角色分工

| 元件             | 責任            |
| -------------- | ------------- |
| Strategy       | 產生 Signal（意圖） |
| Signal         | 策略的「投票」       |
| Position       | 統一狀態機（唯一真相）   |
| PositionPolicy | 多策略協調規則       |
| Execution      | 實際下單          |
| Exchange       | 技術細節          |

**關鍵點：策略永遠不知道其他策略存在**

### 4.3 核心資料流

```text
Market Data
    ↓
┌───────────────┐
│ Strategy A    │──┐
└───────────────┘  │
┌───────────────┐  │
│ Strategy B    │──┼──▶ Signal Aggregator ──▶ Position
└───────────────┘  │
┌───────────────┐  │
│ Strategy C    │──┘
    ↓
Execution → Exchange
```

### 4.4 Domain 設計

#### Signal（策略只產生這個）

```python
# domain/trading/strategies/signal.py
from enum import Enum
from dataclasses import dataclass

class SignalType(Enum):
    BUY = "buy"
    SELL = "sell"
    HOLD = "hold"

@dataclass(frozen=True)
class Signal:
    strategy_id: str
    type: SignalType
    strength: float   # 0.0 ~ 1.0
    confidence: float # 信心值
```

#### Strategy 抽象（互不相知）

```python
# domain/trading/strategies/base.py
from abc import ABC, abstractmethod

class Strategy(ABC):
    id: str

    @abstractmethod
    def generate(self, market_data) -> Signal:
        ...
```

#### Position（唯一真相）

```python
# domain/trading/entities/position.py
class Position:
    def __init__(self):
        self.size = 0.0
        self.avg_price = 0.0

    def apply_intent(self, intent):
        # 只在這裡改倉位
        ...
```

> ❗ **Position 是全系統唯一能改倉位的地方**

### 4.5 Signal Aggregation（多策略協調核心）

#### PositionPolicy（超關鍵）

```python
# domain/trading/services/position_policy.py
class PositionPolicy:
    def aggregate(self, signals: list[Signal]) -> "PositionIntent":
        ...
```

#### 常見 Policy

| Policy          | 說明    |
| --------------- | ----- |
| Majority Vote   | 多數決   |
| Weighted Sum    | 強度加權  |
| Leader Strategy | 主策略優先 |
| Risk Override   | 風控可否決 |

#### PositionIntent（策略 → 持倉的唯一語言）

```python
@dataclass
class PositionIntent:
    target_size: float
    reason: str
```

#### Policy 實作範例（Weighted）

```python
class WeightedPolicy(PositionPolicy):
    def aggregate(self, signals):
        score = sum(
            s.strength * s.confidence *
            (1 if s.type == SignalType.BUY else -1)
            for s in signals
        )

        if score > 0.5:
            return PositionIntent(target_size=1.0, reason="weighted_buy")
        elif score < -0.5:
            return PositionIntent(target_size=0.0, reason="weighted_sell")
        return None
```

### 4.6 Application 層（真正的交易流程）

```python
class MultiStrategyBotService:
    def __init__(self, strategies, position, policy, execution):
        ...

    async def on_market_tick(self, market_data):
        signals = [
            s.generate(market_data)
            for s in self.strategies
        ]

        intent = self.policy.aggregate(signals)
        if not intent:
            return

        order = self.position.apply_intent(intent)
        if order:
            await self.execution.place(order)
```

### 4.7 Execution（永遠不知道策略）

```python
class ExecutionService:
    async def place(self, order):
        await self.exchange.place_order(order)
```

### 4.8 為什麼這個設計「不會互打」

❌ 錯誤設計
> Strategy A 下 BUY  
> Strategy B 下 SELL  
> → 兩邊互砍

✅ 正確設計
> Strategy A 投票 BUY  
> Strategy B 投票 SELL  
> → PositionPolicy 統一裁決

### 4.9 進階玩法

#### 策略權重動態調整
- 根據勝率自動調權重

#### 策略冷卻
- 連敗策略暫停投票

#### 分市場共用 Position
- 多 timeframe → 一個倉位

#### 模擬 / 實盤無縫切換
- Execution Adapter 切換

### 4.10 多策略檢查表

| 問題              | 正確 |
| --------------- | -- |
| 策略知道其他策略嗎       | ❌  |
| 策略會改倉位嗎         | ❌  |
| Position 是唯一狀態嗎 | ✅  |
| 能單獨回測策略嗎        | ✅  |
| 能換交易所嗎          | ✅  |

---

## 五、實時數據流整合

### 5.1 整體即時流程

```text
MEXC WebSocket
    ↓
WS Client (infrastructure)
    ↓
Market Event (DTO / Domain-neutral)
    ↓
Market Stream Dispatcher
    ↓
┌───────────────┐
│ Strategy A    │
├───────────────┤
│ Strategy B    │  → Signals
├───────────────┤
│ Strategy C    │
└───────────────┘
    ↓
PositionPolicy (聚合)
    ↓
Position (唯一狀態)
    ↓
ExecutionService
    ↓
MEXC REST API
```

**核心原則**:
- WS 只推資料，不碰策略
- 策略只算，不碰 WS
- Bot 只協調，不碰 API

### 5.2 Infrastructure：MEXC WS 客戶端

```python
# infrastructure/exchange/mexc/ws_client.py
import json
import websockets

class MexcWSClient:
    def __init__(self, url: str):
        self.url = url

    async def connect(self):
        self.ws = await websockets.connect(self.url)

    async def subscribe_kline(self, symbol: str, interval: str):
        msg = {
            "method": "SUBSCRIPTION",
            "params": [f"spot@public.kline.v3.api@{symbol}@{interval}"],
        }
        await self.ws.send(json.dumps(msg))

    async def listen(self):
        async for message in self.ws:
            yield json.loads(message)
```

✅ **這裡完全不知道 Strategy / Bot / Position 存在**

### 5.3 Market Event 標準化

```python
# application/trading/dtos/market_event.py
from dataclasses import dataclass
from datetime import datetime

@dataclass(frozen=True)
class MarketCandle:
    symbol: str
    open: float
    high: float
    low: float
    close: float
    volume: float
    closed_at: datetime
```

```python
# infrastructure/exchange/mexc/adapters.py
def map_ws_to_candle(payload: dict) -> MarketCandle:
    k = payload["data"]
    return MarketCandle(
        symbol=k["symbol"],
        open=float(k["open"]),
        high=float(k["high"]),
        low=float(k["low"]),
        close=float(k["close"]),
        volume=float(k["volume"]),
        closed_at=datetime.fromtimestamp(k["end"] / 1000),
    )
```

> **WS Payload 永遠不會直接進 Strategy**

### 5.4 Market Stream Dispatcher

```python
# application/trading/services/market_stream.py
class MarketStream:
    def __init__(self, bot):
        self.bot = bot

    async def on_event(self, candle):
        await self.bot.on_market_tick(candle)
```

### 5.5 Strategy（只吃 MarketCandle）

```python
# domain/trading/strategies/base.py
class Strategy:
    id: str

    def generate(self, candle) -> Signal:
        raise NotImplementedError
```

```python
# domain/trading/strategies/ema_cross.py
class EMACrossStrategy(Strategy):
    id = "ema_cross"

    def generate(self, candle):
        if candle.close > self.ema:
            return Signal(self.id, SignalType.BUY, 0.7, 0.8)
        return Signal(self.id, SignalType.HOLD, 0, 0)
```

✅ **策略完全不知道 WS 是即時的**

### 5.6 責任防線

| 層         | 知道什麼    |
| --------- | ------- |
| WS Client | JSON    |
| Adapter   | DTO     |
| Strategy  | Candle  |
| Bot       | Signals |
| Position  | Size    |
| Execution | Order   |
| Exchange  | API     |

**沒有任何一層「跨兩層知道事情」**

### 5.7 為什麼這條線能長期撐住

✅ **可插拔**: 換 WS 來源（回測 / 模擬）不用動策略  
✅ **可擴充**: 新策略只加檔案  
✅ **不亂序**: WS → Dispatcher → Bot 單入口  
✅ **可測試**: Strategy / Policy / Position 全可單測

### 5.8 系統比喻

> **WS 是神經**  
> **Strategy 是大腦皮層**  
> **Position 是中樞神經**  
> **Execution 是肌肉**

---

## 六、生產環境部署

### 6.1 先講結論（非常重要）

#### Cloud Run + WS 的現實限制

1. **Cloud Run 是無狀態的**
2. **WS 連線一定會斷（最長幾十分鐘）**
3. **不能把 WS 當「永遠在線」**

👉 正確做法不是「避免斷線」，而是：

> **WS 是可恢復資料流**  
> **Position 是唯一狀態**  
> **Bot 是可重啟的**

### 6.2 整體最終架構

```text
┌──────────────────────────┐
│   Market Data Source     │
│                          │
│  WS (Live) / Replay /    │
│  CSV / REST Snapshot     │
└───────────▲──────────────┘
            │
┌───────────┴──────────────┐
│   MarketFeed Interface   │  ← 同一介面
└───────────▲──────────────┘
            │
┌───────────┴──────────────┐
│   MarketStream (App)     │
│  - 心跳                  │
│  - 斷線重連              │
│  - 多 timeframe 對齊     │
└───────────▲──────────────┘
            │
┌───────────┴──────────────┐
│   MultiStrategy Bot      │
│  - 多策略                │
│  - 共用 Position         │
└───────────▲──────────────┘
            │
┌───────────┴──────────────┐
│ Position / Execution     │
│ (Live / Paper / Sim)     │
└──────────────────────────┘
```

### 6.3 WS 斷線重連 + 心跳

#### 設計原則

- **WS Client 永遠是「可被殺掉」的**
- 心跳 ≠ WS ping
- **真正的心跳是「資料是否持續前進」**

#### WS Client（Infrastructure）

```python
class MexcWSClient:
    def __init__(self, url, heartbeat_sec=20):
        self.url = url
        self.heartbeat_sec = heartbeat_sec
        self.last_message_at = time.time()

    async def connect(self):
        self.ws = await websockets.connect(self.url)

    async def listen(self):
        async for msg in self.ws:
            self.last_message_at = time.time()
            yield json.loads(msg)

    def is_alive(self):
        return time.time() - self.last_message_at < self.heartbeat_sec
```

#### 重連 Supervisor（Application 層）

```python
class MarketStreamRunner:
    async def run(self):
        while True:
            try:
                await self._run_once()
            except Exception as e:
                logger.warning("WS died, reconnecting", exc_info=e)
                await asyncio.sleep(2)

    async def _run_once(self):
        await self.client.connect()
        await self.client.subscribe()

        async for raw in self.client.listen():
            candle = map_ws_to_candle(raw)
            await self.stream.on_event(candle)

            if not self.client.is_alive():
                raise RuntimeError("heartbeat timeout")
```

👉 **Cloud Run 被重啟 ≠ 交易狀態遺失**  
因為 Position 在 Redis / DB

### 6.4 即時 WS + 多 timeframe + 多策略同步

#### 問題本質

- WS 通常只給 **最小 timeframe（1m）**
- 但策略需要：1m / 5m / 15m / 1h

❌ 錯誤做法：每個 timeframe 各開一條 WS  
✅ 正確做法：**單一 WS → 時間聚合**

#### Timeframe Aggregator（Application）

```python
class TimeframeAggregator:
    def __init__(self, timeframes):
        self.buffers = {tf: [] for tf in timeframes}

    def on_candle(self, candle):
        completed = []

        for tf, buf in self.buffers.items():
            buf.append(candle)
            if self._is_tf_closed(buf, tf):
                completed.append((tf, self._merge(buf)))
                buf.clear()

        return completed
```

#### 同步策略觸發

```python
async def on_candle(self, candle):
    completed = self.aggregator.on_candle(candle)

    for tf, merged_candle in completed:
        await self.bot.on_market_tick(tf, merged_candle)
```

**策略宣告自己要什麼 timeframe**

```python
class Strategy:
    timeframe = "5m"
```

👉 **不同 timeframe 的策略，永遠不會亂序**

### 6.5 同一條線支援 回測 / Paper / 實盤

#### MarketFeed 抽象（核心）

```python
class MarketFeed(ABC):
    async def stream(self):
        yield MarketCandle
```

**實盤（WS）**

```python
class LiveWSFeed(MarketFeed):
    async def stream(self):
        async for raw in self.ws.listen():
            yield map_ws_to_candle(raw)
```

**回測（歷史資料）**

```python
class ReplayFeed(MarketFeed):
    async def stream(self):
        for candle in self.history:
            yield candle
```

#### Execution 抽象（同理）

```python
class ExecutionPort(ABC):
    async def place(self, order): ...
```

| 模式    | Execution      |
| ----- | -------------- |
| 回測    | SimExecution   |
| Paper | PaperExecution |
| 實盤    | MexcExecution  |

👉 **Bot / Strategy / Position 完全不用改**

### 6.6 Cloud Run 實戰注意事項

#### ✅ 必做

- Position / Order 狀態放 Redis / Supabase
- WS 只當行情，不當真相
- 每次啟動先：
  - REST sync account
  - REST sync position

#### ❌ 不要做

- 把 WS 當 state
- 在記憶體存倉位
- 指望 WS 永不斷線

### 6.7 最終「不會翻車」檢查表

| 項目           | 正確    |
| ------------ | ----- |
| Cloud Run 重啟 | 不影響倉位 |
| WS 斷線        | 自動恢復  |
| 多 timeframe  | 單一 WS |
| 多策略          | 同步不互打 |
| 回測 / 實盤      | 同一條線  |

### 6.8 系統設計者的理解

> **交易系統不是「一直跑」**  
> **而是「隨時能再跑」**

---

## 七、完整實現範例

### 7.1 專案架構（Cloud Run 專用 + 多策略）

```text
src/app/
├── domain/
│   └── trading/
│       ├── entities/
│       │   └── position.py
│       ├── strategies/
│       │   ├── base.py
│       │   └── ema_cross.py
│       ├── signal.py
│       └── value_objects.py
│
├── application/
│   └── trading/
│       ├── services/
│       │   ├── trading_bot_service.py
│       │   └── timeframe_aggregator.py
│       ├── dtos/
│       │   └── market_event.py
│       └── ports/
│           └── execution_port.py
│
├── infrastructure/
│   └── exchange/mexc/
│       ├── ws_client.py
│       ├── rest_client.py
│       └── adapters.py
│
├── interfaces/
│   └── cli/
│       └── run_bot.py
│
└── shared/
    ├── config.py
    └── logging.py
```

### 7.2 核心程式碼

#### WS Client（Infrastructure）

```python
# infrastructure/exchange/mexc/ws_client.py
import asyncio, json, time, websockets
from shared.logging import logger

class MexcWSClient:
    def __init__(self, url: str, heartbeat_sec=20):
        self.url = url
        self.heartbeat_sec = heartbeat_sec
        self.last_message_at = time.time()

    async def connect(self):
        self.ws = await websockets.connect(self.url)
        logger.info("WS connected")

    async def subscribe(self, symbol: str, interval: str):
        msg = {
            "method": "SUBSCRIPTION",
            "params": [f"spot@public.kline.v3.api@{symbol}@{interval}"],
        }
        await self.ws.send(json.dumps(msg))

    async def listen(self):
        async for message in self.ws:
            self.last_message_at = time.time()
            yield json.loads(message)

    def is_alive(self):
        return time.time() - self.last_message_at < self.heartbeat_sec
```

#### Market Event DTO（Application）

```python
# application/trading/dtos/market_event.py
from dataclasses import dataclass
from datetime import datetime

@dataclass(frozen=True)
class MarketCandle:
    symbol: str
    open: float
    high: float
    low: float
    close: float
    volume: float
    closed_at: datetime
```

#### Timeframe Aggregator（Application）

```python
# application/trading/services/timeframe_aggregator.py
from datetime import timedelta

class TimeframeAggregator:
    def __init__(self, timeframes):
        self.timeframes = timeframes
        self.buffers = {tf: [] for tf in timeframes}

    def on_candle(self, candle):
        completed = []
        for tf in self.timeframes:
            buf = self.buffers[tf]
            buf.append(candle)
            if self._is_tf_closed(buf, tf):
                merged = self._merge(buf, tf)
                completed.append((tf, merged))
                buf.clear()
        return completed

    def _is_tf_closed(self, buf, tf):
        if not buf:
            return False
        start = buf[0].closed_at
        end = buf[-1].closed_at
        return (end - start) >= timedelta(minutes=tf)

    def _merge(self, buf, tf):
        return buf[-1]  # 簡化，只取最後一根
```

#### Strategy 抽象 + EMA 範例（Domain）

```python
# domain/trading/strategies/base.py
from abc import ABC, abstractmethod

class Strategy(ABC):
    id: str
    timeframe: int

    @abstractmethod
    def generate(self, candle):
        pass
```

```python
# domain/trading/strategies/ema_cross.py
from .base import Strategy
from ..signal import Signal, SignalType

class EMACrossStrategy(Strategy):
    id = "ema_cross"
    timeframe = 5  # 5 分鐘

    def __init__(self):
        self.ema = None

    def generate(self, candle):
        if candle.close > (self.ema or candle.close):
            return Signal(self.id, SignalType.BUY, 0.7, 0.9)
        return Signal(self.id, SignalType.HOLD, 0.0, 0.0)
```

#### Multi-Strategy Bot（Application）

```python
# application/trading/services/trading_bot_service.py
class TradingBotService:
    def __init__(self, strategies, position, policy, execution):
        self.strategies = strategies
        self.position = position
        self.policy = policy
        self.execution = execution

    async def on_market_tick(self, timeframe, candle):
        signals = [
            s.generate(candle)
            for s in self.strategies
            if s.timeframe == timeframe
        ]
        intent = self.policy.aggregate(signals)
        if not intent:
            return
        order = self.position.apply_intent(intent)
        if order:
            await self.execution.place(order)
```

#### CLI Runner（Cloud Run entrypoint）

```python
# interfaces/cli/run_bot.py
import asyncio
from infrastructure.exchange.mexc.ws_client import MexcWSClient
from application.trading.services.trading_bot_service import TradingBotService
from application.trading.services.timeframe_aggregator import TimeframeAggregator
from domain.trading.strategies.ema_cross import EMACrossStrategy

async def main():
    ws_client = MexcWSClient("wss://contract.mexc.com/ws")
    await ws_client.connect()
    await ws_client.subscribe("BTCUSDT", "1m")

    aggregator = TimeframeAggregator([1,5,15])
    strategies = [EMACrossStrategy()]
    bot = TradingBotService(strategies, position, policy, execution)

    while True:
        async for raw in ws_client.listen():
            candle = map_ws_to_candle(raw)
            for tf, merged in aggregator.on_candle(candle):
                await bot.on_market_tick(tf, merged)
        if not ws_client.is_alive():
            await asyncio.sleep(2)
            await ws_client.connect()

if __name__ == "__main__":
    asyncio.run(main())
```

### 7.3 Dockerfile（Cloud Run 專用）

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/

ENTRYPOINT ["python", "src/app/interfaces/cli/run_bot.py"]
```

#### Cloud Run 設置

- **CPU / Memory** 適中：WS 不吃太多 CPU，但 asyncio 需要足夠 memory
- **Concurrency = 1**：每個 WS Runner 單線程
- **最大實例 = 1~3**：避免多個實例競爭同一倉位

### 7.4 特性回顧

1. **WS 斷線自動重連 + 心跳檢測**
2. **單一 WS 支援多 timeframe → 多策略同步觸發**
3. **同一條線同時支援回測 / Paper / 實盤**
4. **完全遵守 Clean Architecture**：策略 / 持倉 / Execution / WS 分離
5. **Cloud Run 無狀態安全**：Position 放 Redis / Supabase

---

## 總結：關鍵原則

### 架構級別

> **能活 3 個月的交易 bot 靠策略**  
> **能活 3 年的交易系統靠架構**

### 設計級別

> **策略是意見**  
> **持倉是法律**  
> **下單是執行**

### 實現級別

> **交易系統不是「一直跑」**  
> **而是「隨時能再跑」**
